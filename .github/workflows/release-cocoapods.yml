name: release-cocoapods

# Publishes Kiahk.podspec to the CocoaPods trunk on every v*.*.* tag.
# Requires the COCOAPODS_TRUNK_TOKEN repo secret (the value of the
# COCOAPODS_TRUNK_TOKEN env var from your `pod trunk register` session,
# or `cat ~/.netrc | grep trunk.cocoapods.org` after registering).
on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to publish (e.g. v0.1.4). Used to validate podspec version + as the git source tag in the published pod."
        required: true
      ref:
        description: "Git ref to checkout (default: same as tag). Use 'master' when fixing the podspec for an already-pushed tag without force-updating the tag."
        required: false
        default: ""

permissions:
  contents: read

jobs:
  publish:
    # CocoaPods publish requires macOS — the trunk push needs a working
    # `pod spec lint` which needs Xcode.
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref != '' && github.event.inputs.ref || github.event.inputs.tag || github.ref }}
          fetch-depth: 0  # we need all tags so the input.tag can be verified to exist

      - name: Verify the git tag exists (when checked out from a different ref)
        if: github.event.inputs.ref != ''
        shell: bash
        run: |
          TAG="${{ github.event.inputs.tag }}"
          if ! git rev-parse --verify "refs/tags/$TAG" >/dev/null 2>&1; then
            echo "::error::Tag $TAG does not exist in git history. Push the tag first."
            exit 1
          fi
          echo "Tag $TAG resolves to: $(git rev-parse refs/tags/$TAG)"

      - name: Resolve release version
        id: ver
        shell: bash
        run: |
          REF="${{ github.event.inputs.tag || github.ref_name }}"
          TAG="${REF#refs/tags/}"
          VERSION="${TAG#v}"
          echo "tag=$TAG"          >> "$GITHUB_OUTPUT"
          echo "version=$VERSION"  >> "$GITHUB_OUTPUT"

      - name: Verify tag matches podspec version
        shell: bash
        run: |
          PODSPEC_VERSION=$(grep -oE "s\.version\s*=\s*'[^']+'" Kiahk.podspec | sed -E "s/.*'(.+)'/\1/")
          if [ "$PODSPEC_VERSION" != "${{ steps.ver.outputs.version }}" ]; then
            echo "::error::Tag version (${{ steps.ver.outputs.version }}) does not match Kiahk.podspec version ($PODSPEC_VERSION)."
            echo "Bump Kiahk.podspec first, commit, then re-tag."
            exit 1
          fi
          echo "Version match: $PODSPEC_VERSION"

      - name: Install CocoaPods
        run: gem install cocoapods --no-document

      - name: Show Xcode + Swift versions (debugging)
        run: |
          xcode-select -p
          xcodebuild -version
          swift --version

      # --swift-version=5.0 is required: the macos-14 runner's Xcode generates
      # its CocoaPods test project with SWIFT_VERSION=6.0 by default, but the
      # Xcode build-setting SWIFT_VERSION only accepts 4.0/4.2/5.0 (Swift 6 is
      # a language mode toggle, not a SWIFT_VERSION value). Forcing 5.0 sidesteps
      # the issue; the actual library code compiles fine under any 5.x+ compiler.
      - name: Lint podspec
        run: pod spec lint Kiahk.podspec --allow-warnings --swift-version=5.0

      - name: Publish to CocoaPods trunk
        env:
          COCOAPODS_TRUNK_TOKEN: ${{ secrets.COCOAPODS_TRUNK_TOKEN }}
        run: pod trunk push Kiahk.podspec --allow-warnings --skip-import-validation --swift-version=5.0

      - name: Summary
        if: success()
        run: |
          echo "### Published Kiahk ${{ steps.ver.outputs.version }} to CocoaPods" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "- CocoaPods: https://cocoapods.org/pods/Kiahk" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Install (Podfile): \`pod 'Kiahk', '~> ${{ steps.ver.outputs.version }}'\`" >> "$GITHUB_STEP_SUMMARY"
